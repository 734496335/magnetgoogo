# Magnet 项目根目录清理与回收归档记录

日期：2026-07-30（UTC+8）

## 结论

`ROOT_CLEANUP=PASS_WITH_REVERSIBLE_RECYCLE`

本轮未删除任何文件。已将确认属于一次性脚本、探测结果、网页快照、ADB/K30S调试产物、历史备份、根目录旧APK和临时目录的内容移动到：

`D:\lpproduct\magnet\.recycle\2026-07-30`

完整恢复索引：

`.recycle/2026-07-30/cleanup-index.md`

## 清理规模

- 根目录文件：822 个移入回收站。
- 根目录目录：14 个移入回收站。
- 回收内容总量：约 294.05 MiB。
- 有效文档重定位：1 个，`_publish_sources_checklist.md` → `docs/project-nebula/SOURCE-RELEASE-CHECKLIST.md`。
- 所有回收项保留原文件名或目录名，索引可确定性还原原路径。

## 分类

- 一次性脚本：251 个。
- 探测、候选、测试和批次结果：423 个。
- HTML/JS网页快照：30 个。
- 调试日志：14 个。
- K30S设备产物：17 个。
- `sources.json.bak_*`：42 个。
- 临时文件：42 个。
- 根目录旧APK：3 个。
- 临时目录：14 个。

## 保护规则

以下内容明确保留：

- 当前正式源码、数据、站点和发布仓库。
- `release.py`、`source_discovery.py` 及正式发现模块。
- `releases/` 正式版本归档及签名材料。
- `magnetgoogo/` GitHub正式运维克隆。
- 被正式代码引用的候选池和辅助输入。
- `.env`、`.wrangler/` 与签名备份。
- 百度验证文件和本地启动入口。

`NUL` 因 Windows 保留设备名语义暂未强制移动；本地AI工具目录也保留观察。

## 反向引用审计

对回收文件名执行了 Git 跟踪内容反向搜索。命中项主要为：

- 历史 `DEV-LOG.md` 证据引用；
- `.gitignore` 中的临时文件规则；
- 可重新生成的报告默认输出路径；
- 已由正式模块取代的旧工具链输入/输出。

当前正式入口 `source_discovery.py` 与 `release.py` 均能正常输出帮助信息；核心门禁通过。

## 验证

- `python validate_enum.py`：`ALL VALID`，仅保留既有 4 个缺少 brand 的警告。
- `python -m pytest magnet/tests/crawler_v3 -m "not integration" -q`：`73 passed, 2 deselected`。
- `cd magnetgoogo-app && npx tsc --noEmit`：PASS。
- `python source_discovery.py --help`：PASS。
- `python release.py --help`：PASS。
- 根目录现在仅保留核心仓库、正式输入、部署/构建入口及明确保护项。

## 删除边界

本轮没有删除。建议至少观察 30 天，再根据以下证据决定是否永久删除：

1. 正式构建、测试和发布流程未出现缺失文件。
2. 回收目录内文件未被人工恢复。
3. Git和代码搜索未新增对回收文件的依赖。
4. 当前候选池、品牌画像和历史正式版本仍完整。
