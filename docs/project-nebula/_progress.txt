Current phase: App v0.2.1 MOVIE COPY POLISH PASS / NOT RELEASED 2026-07-26

## Completed
- Replaced the separate rating strip with one shared movie tag row.
- Tag order is always Douban, IMDb, then quality tags such as 4K and HD.
- Recommended cards, recent-list rows and movie details use the same tag component.
- Missing, zero, non-finite and out-of-range ratings remain hidden.
- Internal 6.0 / 8.0 tiers only control restrained color and font weight.
- Detail content order remains synopsis, movie information, cast, then resources.
- Empty synopsis, movie-information, cast and resource sections are entirely omitted.
- Added a fixed “查看资源（N）” shortcut that scrolls to resources and automatically hides while the section is visible.
- Removed obsolete empty-state copy and the old MovieRatingStrip component.
- Renamed the recommendation section from “值得一看” to “近期好片”.
- Movie titles turn red in recommended cards, recent rows and details when Douban or IMDb is at least 6.0.
- The fixed “查看资源（N）” action now uses a true capsule shape with narrower horizontal bounds.

## Verification
- TypeScript -> PASS.
- App adversarial tests -> 36/36 PASS.
- Movie feed tests -> PASS; fluency suite -> 17/17 PASS.
- `npm run android:k30s` -> BUILD SUCCESSFUL / install Success.
- K30S list showed `豆瓣 7.1` before `4K` and `HD`.
- K30S detail showed a red 7.1-rated title, `豆瓣 7.1 / 4K / HD`, plus the centered capsule `查看资源（3）`.
- Tapping the shortcut scrolled to `资源 / 3 个资源` and three magnet cards.
- The shortcut was absent once the resource section entered the viewport.
- No AndroidRuntime fatal or React Native unhandled error observed.

## Remaining before release
- Run final signed v0.2.1 release acceptance; do not tag, publish or deploy yet.
