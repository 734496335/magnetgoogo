Current phase: resource_index LIVE crawl for javbus.com WORKING 2026-07-25

## Goal achieved
- Live crawl javbus.com → parse content/people/tags/magnets → SQLite resource index
- Multi-source extension via adapters/registry.py (register new adapter + crawler)

## How to use
python -m magnet.resource_index.cli init-db --db ri.db
python -m magnet.resource_index.cli crawl --source javbus --db ri.db --query SSIS --limit 6 --yes
python -m magnet.resource_index.cli crawl --source javbus --db ri.db --detail-url https://www.javbus.com/SSIS-960 --yes
python -m magnet.resource_index.cli show-content SSIS-960 --db ri.db --pretty
python -m magnet.resource_index.cli list-resources SSIS-960 --db ri.db --include-magnets --pretty
python -m magnet.resource_index.cli list-sources

## Verified live (2026-07-25)
- query SSIS limit=2 → 2 contents, 44 resources success
- detail SSIS-960 → 1 content, 21 resources, 2 people, 9 tags success
- unit tests: 46 passed; crawler_v3 68; validate_enum ALL VALID

## Architecture
- LiveHttpClient (curl_cffi chrome124, delay, stop on 403/429/challenge)
- JavBusLiveCrawler: age session → listing/search → detail → AJAX magnets
- ingest_live pipeline + CLI crawl --yes
- registry for future sites (adapter + live crawler factories)

## Next (when needed)
- Add another site: implement adapters/<site>/ + live_crawler, register in registry
- Optional: Postgres / API / App (not required for crawl goal)
