TASK: 生产邮件主动告警（media + 普通版 source-sync）
STATUS: ALERT_HOOKS_PRODUCTION_TESTED / REAL_MAIL_CHANNEL_PENDING
DATE: 2026-08-12 UTC+8

PRODUCTION POLICY:
- media 首次 daily 失败只走原30分钟 retry；retry 再失败才触发 P0。
- source-sync 连续2次小时级失败触发 P1；成功同步后仍连续2次 `<24h` 才触发 P0 临期。
- 首次达到阈值立即通知；未恢复事故 media/source 24h、expiry 12h 低频提醒；恢复只发1次。
- 告警 fail-open；通知系统故障不能改变 media/source 业务退出码。

PRODUCTION PROOF:
- 最新 Resource Index 415 passed / 1 skipped；alert/source targeted 28/28；enum 241 ALL VALID。
- Aliyun Python3.6 / bash / systemd verify PASS；QQ smtp.qq.com:465 TLS NOOP=250。
- 真实 source-sync 故障注入：failures 1→2；恢复后0；生产 full SHA 前后均 `370de74a...`。
- media helper failure→success PASS；source-expiry 23h→23h→25h PASS。
- CRLF 中转故障已留证；alerts/source-sync 强制 eol=lf，Git blob 二进制重部署后生产 CRLF=0。
- `e7125dd` 运行版再次执行正常 source-sync/media success；三类状态均 success/failures=0/open=false。

DELIVERY:
- CloudMonitor 官方支持安全词 URL/token 或 Basic Auth 两种外部报警安全模式；QQ SMTP 授权码 fallback 已实现。
- `/etc/magnet-alerts/alert.env` = 0600 root:root，当前 transport=disabled；目标 QQ 邮箱已配置，SMTP授权码为空。
- 历史 CloudMonitor URL 备份已损坏且确认不含 `https://` token；服务器其他路径无可恢复副本，严格测试未实际发信。
- Cloudflare Email Service 对已验证目标地址可免费发送，但当前 OAuth 缺 email 权限且仍需一次邮箱验证。

REMAINING:
- 重新从 CloudMonitor 联系组复制“接入外部报警”调用地址（安全词模式可免AccessKey），或设置 QQ SMTP授权码；执行 strict test 并确认收件箱实际收到后，CH-009 才能标 solved。
