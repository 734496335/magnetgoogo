Current phase: MEDIA RELEASE M2-B TEMPORARY CREDENTIAL FLOW COMPLETE / FULL REAL R2 RUN AWAITS PARENT CREDENTIALS 2026-07-27

## M2-B implemented
- Added Cloudflare R2 Temporary Credentials API integration for the exact boto3 publisher.
- Parent inputs are read only from `R2_ACCOUNT_ID`, `CLOUDFLARE_API_TOKEN` and `R2_PARENT_ACCESS_KEY_ID`.
- Child credentials are minted in memory with `object-read-write`, one test Bucket, one `m2-test/.../` prefix and a TTL limited to 60-3600 seconds.
- Temporary access key, secret and session token are never serialized, logged, written to receipts or included in exceptions/repr.
- CLI adds `--temporary-credentials --credential-ttl-seconds`; Windows adds `-TemporaryCredentials -CredentialTtlSeconds`.
- Existing direct S3 environment credential mode remains supported.
- Production `v1/current.json` guard and M2 test-prefix guard remain unchanged.

## Verification
- Temporary-credential and publisher targeted tests: 30 passed before final expansion.
- API request contract verified: account endpoint, bucket, parentAccessKeyId, object-read-write, TTL and exact prefix.
- HTTP/API failures expose only status or Cloudflare error codes, never response messages or credential material.
- Missing parent environment fails before local release access or any R2 request.
- Official Cloudflare contract checked: temporary credentials support bucket, permission, prefixes/objects, session token and TTL; the API still requires a parent R2 Access Key ID plus API token.

## Boundary / next step
- Current machine has no direct S3 credentials and no parent API token/access key environment, so a real 616-file boto3 run cannot be fabricated.
- Existing private Bucket `magnetgoogo-media-m2-test` remains non-public, without custom domain or production pointer.
- Once parent variables are supplied, run the Windows publisher with temporary credentials, retain first-upload and second-all-reused receipts, then begin M3 Aliyun mirror.
