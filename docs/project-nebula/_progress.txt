Current phase: MEDIA RELEASE M1 COMPLETE / M2 NOT STARTED 2026-07-26

## M1 implemented
- Added backend-independent `media-current/1` and `media-manifest/1` local release protocol.
- Split 100 movies + 100 series into card, detail, resource and content-addressed cover objects.
- Added deterministic canonical JSON, SHA-256 inventory and Ed25519 pointer/manifest signatures; key initialization is idempotent and can repair the public half from the private key.
- Immutable releases live under `staging/releases/<release_id>`; mutable pointer candidates live separately under `staging/pointers`.
- Same data reuses the exact release and pointer; a higher pointer revision reuses objects but creates a new signed pointer.
- Pointer revisions are monotonic and cannot be reassigned; OS-backed locking rejects concurrent local publishers.
- Added local one-click builder and independent verifier for the existing Windows portable runtime.
- Existing crawler CLI remains usable when the signing dependency is absent; the release wrapper upgrades only its runtime when needed.

## Quality and evidence
- Real build: 100 movies, 100 series, 1,682 unique resources, 200 covers, 200 details, 14 catalog objects.
- Final release: `20260726T000000Z-b8c702d5`; Manifest SHA-256 `8891347a02646fe6d98279205b0614a6945238e5cb57d67188c722febd91f838`.
- Independent verification passed all 614 referenced objects and typed card/detail/resource/cover reference closure.
- Quality: 0 duplicate IDs/resources, 0 cross-season resources, 0 malformed country/genre values, complete covers.
- Series identity: 52 collection/cloud resources, 142 title-inferred resources, 1 genuinely unknown resource.
- Targeted release tests: 21 passed; complete resource-index suite: 143 passed.

## Boundaries / next batch
- No R2 bucket, Aliyun upload, GitHub/Pages/Worker pointer update, App network Feed, push, tag or production deployment was performed.
- `mg-data` versus `maggoogo-sources` remains a control-plane configuration drift to close before M5, not part of the data protocol.
- Next: M2 implement `PublisherBackend` + R2 test bucket/custom-domain upload and remote hash verification without switching the App.
