Current phase: App v0.2.1 FEATURE BASELINE / NOT RELEASED 2026-07-25

## Included in this baseline
- Background search native reliability is K30S accepted.
- Immediate Home handoff, early-return hydration and terminal recovery pass.
- Strict nonzero `{query, token}` identity prevents stale snapshot restore.
- Randomized 31-bit tokens prevent cross-process token reuse.
- New owner fencing prevents stale writes and old-result overwrite.
- A→B foreground-service race and `ForegroundServiceDidNotStartInTimeException` are fixed.
- Startup source sync is single-flight; search/storage/config/classification/sort/UI defects are hardened.
- Native background implementation is represented by a tracked Expo config plugin and templates.

## Verification
- App adversarial suite -> 31/31 PASS.
- Fluency suite -> 17/17 PASS; `npx tsc --noEmit` -> PASS.
- Android export -> 1401 modules, HBC 4.73 MB.
- Gradle test/build -> BUILD SUCCESSFUL, 495 tasks.
- K30S early return and A→B replacement both pass; no residual services or notification.

## Version / Git
- Development version metadata: `0.2.1`.
- Branch: `feature/app-v0.2.1-hardening`.
- This is a feature baseline only: no Release tag, APK upload or remote deployment.

## Next scope before v0.2.1 release
- Add the “资源” module for displaying crawled latest resources and linking into search.
- Complete resource-module integration, full regression and release acceptance.
- Separate residual risks: lock-screen/deep-sleep/process-kill endurance and isolated clean prebuild.
