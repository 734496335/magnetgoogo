TASK: 影视发布 revision 生命周期修复 + Aliyun 源包自动传播闭环
STATUS: PRODUCTION_RECOVERED / REVISION11_LIVE / ALIYUN_SOURCE_SYNC_AUTOMATED
DATE: 2026-08-11 UTC+8

P0 ROOT CAUSE:
- audit/candidate 与正式 publish 共用 releases/staging/pointers，都会按公网 revision+1 写永久 pointer candidate。
- 2026-08-05 audit 留下 11 -> 20260805T000000Z-8013b446；后续新内容 publish 仍请求 revision 11，被不可重绑定安全门禁持续阻断。
- 既有 448-test/live-drill 覆盖单次安全性、同内容重跑、锁/重试，但缺少“audit占N+1 -> 次日内容变化 -> publish N+1”的跨运行状态机序列。

P0 FIX/LIVE:
- candidate/audit release 改为 runs/<run_id>/release-candidate，永不占生产 pointer 命名空间。
- publish 前以公网 current 为 authority，签名验证并归档所有 > public revision 的未晋级 pointer 到本次 run evidence；同 revision 冲突仍硬停。
- 旧 revision11 墓碑已归档；正式 revision11 / 20260811T000000Z-8fc684c7 已双端上线。
- Production counts: 274 movies / 299 series / 4238 magnets；R2/Aliyun current + Manifest 字节一致。
- 发布后 audit 生成 run-scoped revision12 candidate；global pointers 仍只有 9/10/11。
- 再跑 same-content publish: no_change=true, public_verified=true, revision 保持11。

P1 ROOT CAUSE/FIX:
- 上次修复自动化了5个 authority-following 端点，但 Aliyun 仍只是一次性人工 SCP；点时收敛测试未覆盖“下一次续期无人值守”。
- Aliyun 无法稳定直连 GitHub Raw；新增每小时 systemd sync，从 magnetgoogo.com authority Worker + jsDelivr 双路取证，结构校验+字节一致后原子替换。
- Aliyun full/green 已更新为 427d490a... / d1c65ef0...；二次运行明确 already current。

GATES:
- Commit 13a6d55908b35e02f8a6e9706604122b9f8d7d51 on feature/media-daily-automation.
- Resource Index 383 passed / 1 skipped; targeted 53/53; compileall PASS; enum 241 ALL VALID.
- New production image b26acfbc12e2... is latest; old image retained as pre-pointer-fix-20260811.
