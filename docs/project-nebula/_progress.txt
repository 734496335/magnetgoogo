Current phase: MEDIA RELEASE M2-C DRY-RUN PREFLIGHT COMPLETE / REAL CREDENTIALED R2 RUN STILL EXTERNALLY BLOCKED 2026-07-27

## M2-C implemented
- Extracted `build_media_publish_plan` from the live publisher so local verification and remote execution share one request contract.
- Added credential-free CLI/Windows `--dry-run` / `-DryRun` mode.
- Dry-run verifies signatures, Manifest hash, all local object hashes, safe paths and production-pointer guards before producing a plan.
- The plan reports release ID, pointer revision, total files, total bytes, object kinds, first/last keys and whether the pointer candidate is included.
- Dry-run performs zero remote requests, creates no receipt and needs neither `--yes` nor Cloudflare/R2 credentials.
- Live publication still consumes the exact same request plan, avoiding drift between preflight and execution.

## Verification
- Publisher and temporary-credential targeted suite: 34 passed.
- Real release dry-run: 614 immutable objects + Manifest + signed pointer candidate = 616 files.
- Real release total planned bytes: 11,072,715.
- Object kinds: 14 catalog, 200 cover, 200 detail, 200 resources, 1 Manifest, 1 pointer candidate.
- `remote_requests=0`, `current_promoted=false`, and no `v1/current.json` key exists in the plan.

## Boundary / next step
- Current machine still lacks both direct R2 S3 credentials and parent temporary-credential inputs, so the real boto3 616-file upload remains externally blocked.
- Existing private test Bucket remains non-public and without a custom domain.
- Once credentials are supplied, execute the same plan twice and retain first-upload plus all-reused receipts; then proceed to M3 Aliyun mirror.
