TASK: 修复 v0.2.5 资源页进入后不自动更新、依赖手动下拉的问题
STATUS: ROOT_CAUSE_FIXED / CODE_GATES_PASS / ANDROID_BUILD_PASS / K30S_UI_ACCEPTANCE_PENDING
DATE: 2026-08-14 UTC+8

ROOT CAUSE:
- resources.tsx 用 backgroundSyncStarted 永久记录“已尝试同步”。
- 首次后台同步失败也不清除标记，后续进入资源页不再自动重试。
- 即使首次成功，Tabs 长驻后 movie/series 每个 kind 也只自动检查一次；以后数天重新进入仍可显示旧缓存。

FIX:
- 缓存/本地 feed 仍立即展示，不阻塞页面。
- Resource Tab 每次重新 focus 自动 revalidate；App 从后台回 active 时也自动 revalidate。
- ResourceAutoSyncGate：成功后60s防抖；失败不进入冷却，下一次 focus/foreground立即可重试；movie/series独立 single-flight。
- 手动下拉保留，并与自动同步共享成功冷却，避免重复网络请求。

VERIFIED:
- resource-auto-sync 行为测试 6/6 PASS。
- TypeScript npx tsc --noEmit PASS。
- App adversarial 36/36 PASS（clean prebuild 后）。
- resource-feed M1-M7 PASS；M8仅因本地未跟踪 bundle 缺失而 SKIP。
- release-build contract PASS。
- 短路径 Android arm64 debug 全链 BUILD SUCCESSFUL；K30S 并存安装仅被 MIUI 用户确认取消拦截，未卸载/覆盖正式 App。

BOUNDARY:
- media-network/security 在 clean worktree 缺历史 untracked release fixture，未进入断言，已留失败证据。
- validate_enum 基线已有 meta.total_rules 不一致，本修复未改 sources.json。
- 代码提交 66376ba；待推独立修复分支并同步运行时文件到当前主工作区。
