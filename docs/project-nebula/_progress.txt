Current phase: App v0.2.1 MOVIE RATING LABELS PASS / NOT RELEASED 2026-07-26

## Completed
- App work only; no crawler, SQLite schema or data re-fetch changes are included.
- Movie list cards now show IMDb and Douban ratings as separate values.
- Missing, non-finite, zero and out-of-range ratings are hidden.
- Any available rating from 6.0 to 7.9 marks the movie as “精品”.
- Any available rating at 8.0 or above upgrades the movie to “高分”.
- “精品” uses a restrained warm emphasis; “高分” uses red extra-bold emphasis.
- Recommended cards and recent-list cards share the same rating logic.
- Feed parsing remains backward-compatible when imdb_rating fields are absent.

## Verification
- TypeScript -> PASS.
- Movie feed tests -> PASS, including 0 / 5.9 / 6.0 / 7.9 / 8.0 boundaries.
- App adversarial tests -> 35/35 PASS.
- Existing local bundle remains 50 movies / 9 recommendations / 134 resources / 50 covers.
- Existing bundle currently has 8 movies in the 6.0–7.9 “精品” tier and no 8.0+ item.
- Android arm64 debug -> BUILD SUCCESSFUL; K30S install -> Success.

## Remaining before release
- IMDb numeric ratings depend on the separate crawler/data pipeline providing imdb_rating.
- Run final signed v0.2.1 release acceptance; do not tag, publish or deploy yet.
