TASK: 生产邮件主动告警（media + 普通版 source-sync）
STATUS: ALERT_STATE_MACHINE_LIVE / DELIVERY_PROVIDER_PENDING
DATE: 2026-08-11 UTC+8

PRODUCTION ALERT LOGIC:
- media 首次 daily 失败只走原30分钟 retry；retry 再失败才触发 P0。
- source-sync 连续2次小时级失败才触发 P1；full 包连续2次剩余<24h 才触发 P0。
- 同一 open incident：media/source 24h 最多重复1次，expiry 12h；恢复仅在此前真实通知成功后发送1次。
- 告警 helper 全部 fail-open；发送故障不能反向让 media/source 主任务失败。

DELIVERY:
- 首选 CloudMonitor External Alert：支持 URL + Security Word，不要求在 ECS 保存阿里云 AK。
- QQ SMTP 授权码 fallback 已实现；阿里云到 smtp.qq.com:465 TLS1.3/证书验证 PASS。
- `/etc/magnet-alerts/alert.env` = 0600 root:root，transport 当前 disabled；服务器不存用户 QQ 邮箱。
- 实际发信仍需 CloudMonitor 控制台一次性激活联系人并生成 External Alert URL/安全词，或提供 QQ SMTP 授权码。

VERIFICATION:
- remote branch `feature/media-daily-automation` = e7125dd。
- Resource Index 414 passed / 1 skipped；alert/deploy targeted 40/40；enum 241 ALL VALID。
- Linux alert/source-sync host files全部 LF；Python3.6/bash/systemd verify PASS。
- 生产 source-sync 正常：full SHA 370de74a...，剩余约64.9h；success helper 无误报。
- 生产 dry-run：source 第1次抑制→第2次通知→24h抑制/重发→恢复单次；media failure/recovery PASS。

BLOCKER:
- 尚未收到真实测试邮件，因此 CH-009 保持 piloting，不标 solved。
