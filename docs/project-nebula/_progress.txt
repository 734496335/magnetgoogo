Current phase: App v0.2.1 RESOURCE TAB K30S PASS / NOT RELEASED 2026-07-25

## Completed
- Bottom navigation now has exactly three tabs: Search, Resources and Settings.
- Original search home remains the Search tab; original settings functions remain the Settings tab.
- Resources tab renders a two-column ranked feed with cover, code, title, people, date, duration and link count.
- Tapping a resource records history and enters the existing `/search?q=<content_code>` flow.
- Resource feed loading is remote-first with a bundled Android asset fallback.
- Strict feed parsing rejects invalid shape, count mismatch and non-continuous ranking.
- Android config plugin injects only `javbus_latest_100_feed.json`; SQLite is not packaged.
- Android development build number is now versionCode 5; app version remains 0.2.1.

## Data boundary
- Local feed passes: 100 source records / 97 canonical contents / 3 duplicate observations.
- Feed contains 299 unique resources and 309 resource observations; zero records lack resources.
- `/data/resource_index/` is Git-ignored; feed, URL snapshot and SQLite remain uncommitted.
- `resourceFeedUrl` is intentionally empty, so the current build shows the bundled snapshot.

## Verification
- TypeScript -> PASS; App adversarial suite -> 33/33 PASS.
- Fluency suite -> 17/17 PASS; resource feed suite -> PASS.
- Expo Android export -> 1406 modules, HBC 4.76 MB.
- Gradle unit/debug build -> BUILD SUCCESSFUL; plugin bundled 100 items.
- K30S install -> Success, versionCode 5.
- K30S UI shows three tabs and “内置快照 / 最新100条”; first card is MY-1065.
- Tapping MY-1065 started the existing SearchKeepAlive search path; no fatal crash.

## Remaining before release
- Deploy the feed to stable HTTPS and configure `resourceFeedUrl` for live refresh.
- Run final v0.2.1 full release acceptance; SQLite-backed in-app detail is deferred.
