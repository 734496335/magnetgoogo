# Project Nebula Handoff

## 1. Current State

- `sources.json` current baseline: `112` total rules, `17 green`, `26 yellow`, `69 gray`.
- This round's work concentrated on the generic funnel rather than `sources.json` writing.
- The funnel can now keep more high-potential candidates alive through Stage3, but conversion remains blocked by site-specific interaction and result-page follow-through.

## 2. Most Relevant Files

- `D:\lpproduct\magnet\magnet\funnel_pipeline.py`
  - Main orchestration.
  - Review these parts first: `CandidateInput`, `candidate_has_magnet_signal`, `extract_evidence`, `extract_detail_urls`, `follow_interstitial_http`, `browser_recovery_worker`, Stage3 interactive search.
- `D:\lpproduct\magnet\magnet\funnel_config.py`
  - Aggressive default budgets, Stage0 retries, Stage3 reserve.
- `D:\lpproduct\magnet\magnet\build_strong_candidates.py`
  - Description-based scoring over `btmayi_real_domains.json`.

## 3. Reports Worth Reading First

- `D:\lpproduct\magnet\highpot_funnel_report_sample_v6.json`
  - Best compact sample of current Stage3 behavior and debug signals.
- `D:\lpproduct\magnet\single_extratorrent_report.json`
  - Clear example of interstitial follow ending in parking-style pages.
- `D:\lpproduct\magnet\strong_candidates_report.json`
  - Shows what happens after description-based candidate prioritization.

## 4. Candidate Pools

- `D:\lpproduct\magnet\funnel_candidates_high_potential.json`
  - Existing high-priority working set.
- `D:\lpproduct\magnet\strong_candidates.json`
  - Built from `btmayi_real_domains.json`, 46 description-strong entries.
- `D:\lpproduct\magnet\current_domains_candidates.json`
  - Newly assembled extra current candidates; not yet uniformly re-run through funnel.

## 5. Confirmed Technical Findings

- The limiting factor is no longer just timeout budget. More candidates now reach Stage3.
- Some domains that look like search sites are actually navigation pages. Example: `torrent2.top`.
- Some legacy brand domains are polluted by interstitial or parking behavior. Example: `extratorrent.ag`.
- Metadata scoring helps narrow the queue, but does not replace live page validation.

## 6. Recommended Read Order

1. `docs/project-nebula/DEV-LOG.md` top two entries
2. `docs/project-nebula/FAST-DISCOVERY-FUNNEL.md`
3. `magnet/funnel_pipeline.py`
4. `highpot_funnel_report_sample_v6.json`
5. `single_extratorrent_report.json`

## 7. Recommended Next Engineering Work

1. Run one focused pass on `current_domains_candidates.json` to generate a fresh debug sample set.
2. Add pre-filtering for pseudo-search navigation pages before Stage2/Stage3 budget is spent.
3. Add stronger Stage3 result-click and post-click wait logic.
4. If continuing generic funnel work, prioritize persistent-yellow site adapters instead of only increasing time budgets.

## 8. Validation Commands

```bash
python -m py_compile magnet/funnel_pipeline.py magnet/funnel_config.py magnet/funnel_report_summary.py magnet/funnel_sources.py
python validate_enum.py
python magnet/build_strong_candidates.py --input btmayi_real_domains.json --output strong_candidates.json
```
